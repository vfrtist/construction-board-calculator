Done
-Create database schema


General Tasks
-Populate database
-Create debounce and direct saving methods for data execution.
-Create header


Manager
-Design project manager screen.
    -Recents
    -Templates
    -... Something else?
        -Can you delete a project from here? Do we care about that?
-Design templates and how to use them.
    -If you open a template how does one convert that into a real project:
        -Save as prompt on edit
        -Save as prompt on open
        -What makes that feel "mobile"
    -Does this mean projects need read/write modes?
    -Relative to the database "templates" will need a universal ID to make their call not unique to the user. Any new templates would be pushed to all users.


Logon
-Design screen.
-Implement log on procedure and storing of user information....
    -Again I can't tell if this is context or some other we based local storage cookies something.


Project
-*low priority* Rename and re-folder "Card" pieces into ProjectCard pieces. They are not generic.
-Re-route loading a project from memory into state, as rendering an API from state.


Visuals
-Add subtle saving ui (think video game book writing animation) probably in the header. Possibly floating.
-Find more references
-Make icons


SQL
-Queries needed:
    -Select all projects for a specific user
    -Select data from a specific project
    -Overwrite data for a specific project
    -Create new project from a template